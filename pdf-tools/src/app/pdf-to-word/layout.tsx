import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "PDF to Word — Convert to DOCX Online Free",
  description: "Convert PDF to editable Word documents. High-fidelity DOCX conversion.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

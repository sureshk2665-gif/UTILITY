"use client";
import ComingSoonTool from "@/components/ComingSoonTool";
export default function PdfToWord() {
  return <ComingSoonTool title="PDF to Word" description="Convert PDF documents to editable DOCX files." color="#3498DB"
    features={["High-fidelity conversion", "Preserves formatting and layout", "Table and image extraction", "Batch conversion support"]} />;
}

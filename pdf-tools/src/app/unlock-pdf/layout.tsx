import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Unlock PDF Online - Remove Password Protection | PDF Tools",
  description: "Remove password protection from your PDF documents. Free online PDF unlock tool.",
  keywords: ["unlock pdf", "remove pdf password", "pdf password remover", "unprotect pdf", "decrypt pdf"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Repair PDF Online - Fix Corrupted PDF Files | PDF Tools",
  description: "Fix damaged or corrupted PDF files and recover content. Free online PDF repair tool.",
  keywords: ["repair pdf", "fix corrupted pdf", "pdf recovery", "broken pdf fix", "damaged pdf repair"],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

"use client";
import ComingSoonTool from "@/components/ComingSoonTool";
export default function HtmlToPdf() {
  return <ComingSoonTool title="HTML to PDF" description="Convert any webpage to PDF by URL." color="#3498DB"
    features={["Full page capture", "CSS rendering support", "Custom page size", "Header and footer options"]} />;
}

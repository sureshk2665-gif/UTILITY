import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "PDF to PowerPoint — Convert to PPTX Free",
  description: "Convert PDF to PowerPoint presentations. Editable PPTX output.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
